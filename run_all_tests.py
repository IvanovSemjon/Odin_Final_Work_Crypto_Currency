#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Главный файл для запуска всех тестов конвертера криптовалют

Этот модуль объединяет и координирует выполнение всех типов тестов:
- Unit-тесты (основные функциональные тесты)
- Интеграционные тесты (тесты взаимодействия компонентов)

Обеспечивает:
- Последовательный запуск всех тестовых наборов
- Красивый и информативный вывод результатов
- Обработку ошибок и исключений
- Сводную статистику по всем тестам
- Рекомендации по дальнейшим действиям

Использование:
    python run_all_tests.py
    
Или импорт функции main() для использования в других скриптах.
"""

import sys
import os
from datetime import datetime
import traceback

# Добавляем путь к тестовым модулям для корректного импорта
# Это необходимо для работы независимо от текущей директории запуска
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

# Импортируем функции запуска тестов из соответствующих модулей
# Каждый модуль содержит свою функцию запуска с обработкой результатов
from test_crypto_currency import run_tests
from test_integration import run_integration_tests


def print_header():
    """
    Выводит красивый заголовок системы тестирования
    
    Включает:
    - Название системы тестирования
    - Текущую дату и время запуска
    - Декоративное оформление
    """
    print("🚀 КОМПЛЕКСНАЯ СИСТЕМА ТЕСТИРОВАНИЯ КОНВЕРТЕРА КРИПТОВАЛЮТ")
    print(f"📅 Дата и время запуска: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")
    print(f"🖥️  Операционная система: {os.name}")
    print(f"🐍 Версия Python: {sys.version.split()[0]}")
    print("=" * 80)


def print_stage_separator(stage_number, stage_name, description):
    """
    Выводит разделитель между этапами тестирования
    
    Args:
        stage_number (int): Номер этапа
        stage_name (str): Название этапа
        description (str): Описание этапа
    """
    print(f"\n📋 ЭТАП {stage_number}: {stage_name}")
    print(f"📝 {description}")
    print("-" * 60)


def print_success_summary():
    """
    Выводит сводку об успешном завершении всех тестов
    
    Включает:
    - Поздравление с успешным прохождением
    - Подтверждение готовности системы
    - Рекомендации по дальнейшим действиям
    """
    print("\n" + "=" * 80)
    print("🎉 ВСЕ ТЕСТЫ ЗАВЕРШЕНЫ УСПЕШНО!")
    print("✅ Конвертер криптовалют прошел полную проверку")
    print("🔒 Система готова к безопасному использованию")
    print("📊 Все компоненты функционируют корректно")
    print("\n🚀 РЕКОМЕНДАЦИИ:")
    print("   • Система готова к продакшену")
    print("   • Можно запускать основное приложение")
    print("   • Рекомендуется периодически повторять тестирование")
    print("=" * 80)


def print_error_summary(error):
    """
    Выводит информацию об ошибке при выполнении тестов
    
    Args:
        error (Exception): Исключение, которое произошло
    """
    print(f"\n❌ КРИТИЧЕСКАЯ ОШИБКА ПРИ ЗАПУСКЕ ТЕСТОВ")
    print("=" * 80)
    print(f"🔥 Тип ошибки: {type(error).__name__}")
    print(f"💬 Сообщение: {str(error)}")
    print("\n📋 ДЕТАЛИ ОШИБКИ:")
    print(traceback.format_exc())
    print("\n🔧 РЕКОМЕНДАЦИИ ПО УСТРАНЕНИЮ:")
    print("   • Проверьте наличие всех зависимостей")
    print("   • Убедитесь в корректности путей к файлам")
    print("   • Проверьте права доступа к файлам")
    print("   • Убедитесь в наличии интернет-соединения (для API тестов)")
    print("=" * 80)


def main():
    """
    Главная функция для координированного запуска всех тестов
    
    Выполняет:
    1. Инициализацию и вывод заголовка
    2. Последовательный запуск всех типов тестов:
       - Unit-тесты (основные функциональные тесты)
       - Интеграционные тесты (тесты взаимодействия)
    3. Обработку ошибок и исключений
    4. Вывод итоговой статистики и рекомендаций
    
    Returns:
        int: Код возврата (0 - успех, 1 - ошибка)
        
    Raises:
        SystemExit: При критических ошибках выполнения
    """
    try:
        # ИНИЦИАЛИЗАЦИЯ: Выводим заголовок системы тестирования
        print_header()
        
        # ЭТАП 1: Запуск основных функциональных тестов (Unit-тесты)
        # Эти тесты проверяют отдельные функции и компоненты изолированно
        print_stage_separator(
            1, 
            "Основные функциональные тесты (Unit-тесты)",
            "Проверка отдельных функций и компонентов системы"
        )
        
        # Вызываем функцию запуска unit-тестов
        # Она выполнит все тесты из test_crypto_currency.py и выведет результаты
        run_tests()
        
        # ЭТАП 2: Запуск интеграционных тестов
        # Эти тесты проверяют взаимодействие между компонентами
        print_stage_separator(
            2,
            "Интеграционные тесты", 
            "Проверка взаимодействия между компонентами системы"
        )
        
        # Вызываем функцию запуска интеграционных тестов
        # Она выполнит все тесты из test_integration.py и выведет результаты
        run_integration_tests()
        
        # ЗАВЕРШЕНИЕ: Выводим сводку об успешном завершении
        print_success_summary()
        
        # Возвращаем код успешного завершения
        return 0
        
    except KeyboardInterrupt:
        # Обработка прерывания пользователем (Ctrl+C)
        print("\n⚠️  ТЕСТИРОВАНИЕ ПРЕРВАНО ПОЛЬЗОВАТЕЛЕМ")
        print("=" * 80)
        return 1
        
    except ImportError as e:
        # Обработка ошибок импорта (отсутствующие модули)
        print(f"\n❌ ОШИБКА ИМПОРТА: {e}")
        print("💡 Убедитесь, что все файлы тестов находятся в правильной директории")
        print("=" * 80)
        return 1
        
    except Exception as e:
        # Обработка всех остальных ошибок
        print_error_summary(e)
        return 1


# Точка входа для запуска системы тестирования
if __name__ == "__main__":
    """
    Точка входа при запуске файла напрямую
    
    Запускает главную функцию main() и обрабатывает код возврата:
    - 0: Все тесты прошли успешно
    - 1: Произошли ошибки при выполнении тестов
    
    Использование:
        python run_all_tests.py
    """
    # Запускаем главную функцию и получаем код возврата
    exit_code = main()
    
    # Завершаем программу с соответствующим кодом
    # Это позволяет другим программам (например, CI/CD системам) 
    # определить успешность выполнения тестов
    sys.exit(exit_code)